import type { Actions, PageServerLoad } from "./$types";

export const _order = 8.0;

export const load: PageServerLoad = async ({ fetch, url }) => {
  return {

  }
}

export const actions: Actions = {
  async make({ request }) {
    return {

    }
  }
}
